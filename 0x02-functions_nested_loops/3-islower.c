#include "main.h"
/**
 * _islower - check for lower characters
 * @c: will be used
 * return: 1 for c and 0 for any character
 */
int _islower(int c)
{
if (c > 96 && c < 123)
{
return (1);
}
return (0);
}
